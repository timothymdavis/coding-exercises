<?xml version="1.0" encoding="UTF-8"?>
<s:beans
    xmlns="http://camel.apache.org/schema/spring"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:s="http://www.springframework.org/schema/beans"
    xsi:schemaLocation="
    http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://camel.apache.org/schema/spring
    http://camel.apache.org/schema/spring/camel-spring.xsd"
    >

  <s:bean id="exception" class="java.lang.RuntimeException" />

  <camelContext>

    <onException>
      <exception>java.lang.Exception</exception>
      <handled><constant>true</constant></handled>
      <log message="An unexpected exception has occured." loggingLevel="ERROR" />
      <to uri="mock:error" />
    </onException>

    <route id="route1">
      <from uri="direct:route1" />
      <split parallelProcessing="false" stopOnException="true">
        <xpath>//SomeElement</xpath>
        <multicast parallelProcessing="false" stopOnException="true">
          <to uri="direct:route2" />
        </multicast>
      </split>
      <log message="Successfully Finished!" loggingLevel="INFO" />
      <to uri="mock:success" />
    </route>

    <route id="route2">
      <from uri="direct:route2" />
      <throwException ref="exception" />
    </route>

  </camelContext>

</s:beans>
